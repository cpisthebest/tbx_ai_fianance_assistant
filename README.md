# Technology

Python, FASTAPI, PostgreSQL, pgvector, Ollama, FlashRank

## Execution Steps

Generate embeddings - python -m app.services.index_service

backend command - uvicorn app.main:app --reload

frontend command - streamlit run app/frontend/streamlit_app.py

